name: Download Asset QR PDF

on:
  workflow_dispatch:
    inputs:
      org_id:
        required: true
        default: "383177088934346755"
      asset_id:
        description: Asset UUID (leave empty to pick first active)
        required: false
        default: ""

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: GET /assets/{id}/qr.pdf on VPS catalog
        env:
          HOST: ${{ secrets.DEPLOY_HOST }}
          USER: ${{ secrets.DEPLOY_USER }}
          KEY: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          ORG_ID: ${{ inputs.org_id }}
          ASSET_ID: ${{ inputs.asset_id }}
        run: |
          set -euo pipefail
          install -m 600 /dev/null /tmp/deploy_key
          printf '%s\n' "$KEY" > /tmp/deploy_key
          ssh -i /tmp/deploy_key -o StrictHostKeyChecking=no "${USER}@${HOST}" bash -s <<EOF
          set -euo pipefail
          ORG_ID='${ORG_ID}'
          ASSET_ID='${ASSET_ID}'
          if [[ -z "\$ASSET_ID" ]]; then
            ASSET_ID=\$(curl -sS -H "X-Org-Id: \$ORG_ID" http://127.0.0.1:8091/assets \
              | python3 -c 'import json,sys; items=json.load(sys.stdin).get("items") or []; act=[a for a in items if a.get("status")=="active"]; print(act[0]["id"] if act else "")')
          fi
          test -n "\$ASSET_ID"
          echo "ASSET_ID=\$ASSET_ID"
          curl -fsS -H "X-Org-Id: \$ORG_ID" -H "X-User-Id: smoke-ops" \
            "http://127.0.0.1:8091/assets/\${ASSET_ID}/qr.pdf" -o /tmp/asset-qr.pdf
          test "\$(dd if=/tmp/asset-qr.pdf bs=5 count=1 2>/dev/null)" = "%PDF-"
          test "\$(wc -c < /tmp/asset-qr.pdf)" -gt 100
          EOF
